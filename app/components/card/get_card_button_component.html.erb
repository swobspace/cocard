<%= button_to(raw(%Q[<i class="fa-solid fa-sim-card"></i>]),
              get_card_card_path(card, context_id: context.id),
              title: t('cocard.get_card'),
              class: 'btn btn-warning me-1')
 %>

