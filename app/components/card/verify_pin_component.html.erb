<%= button_to(raw(%Q[<i class="fa-solid fa-rotate-right"></i>]),
              verify_pin_card_path(card, context_id: context.id),
              title: t('cocard.verify_pin'),
              class: "btn btn-sm btn-danger me-1")
 %>
