<%= button_to(raw(%Q[<i class="fa-solid fa-thumbtack"></i>]),
              get_pin_status_card_path(card, context_id: context.id),
              title: t('cocard.get_pin_status'),
              class: "btn btn-warning me-1 #{pin_color}")
 %>
