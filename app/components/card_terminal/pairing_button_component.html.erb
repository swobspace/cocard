<%= button_to(raw(%Q[<i class="fa-solid fa-fw fa-handshake"></i>]), 
              remote_pairing_card_terminal_path(card_terminal),
      method: 'post',
      class: 'btn btn-warning me-1',
      title: title,
      form: {
        data: {
          "turbo-confirm": confirmation
        }
      }
    )
 %>
