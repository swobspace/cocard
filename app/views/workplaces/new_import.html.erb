<%= render FormComponent.new(title: t('wobapphelpers.actions.new_import',
                             model: t('workplaces',
                             scope: 'controller'))) do %>
  <%= render 'import_form' %>

  <div class="mt-3 p-2 bg-light">
    <p>Die CSV-Datei muss genau zwei Spalten haben mit folgendem Header:</p>
    <pre>name;description</pre>
    <p>Trennzeichen ist das Semikolon, der Text darf nicht Anführungszeichen eingeschlossen sein</p>
  </div>
<% end %>
