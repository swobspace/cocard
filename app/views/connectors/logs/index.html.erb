<%= turbo_frame_tag('logs') do %>
 <% if @logs.any? %>
  <div class="card mt-3">
    <div class="card-header">
      <div class="row">
        <div class="col-xs-5 col-md-4 col-ml-3">
          <h4 class="me-4 pt-1">Logs</h4>
        </div>
        <div class="d-flex col-xs-7 col-md-8 col-ml-9">
        </div>
      </div>
    </div>
    <div>
      <table class="table table-sm">
        <thead>
          <tr>
            <th class=""><%= t('attributes.since') %></th>
            <th><%= t('attributes.level') %></th>
            <th><%= t('attributes.action') %></th>
            <th><%= t('attributes.message') %></th>
            <th class="text-center"><%= t('attributes.is_valid') %></th>
            <th class="text-end"><%= t('attributes.last_seen') %></th>
            <th class="nosort text-end"><%= t('cocard.action') %></th>
          </tr>
        </thead>
        <tbody>
          <%= render @logs %>
        </tbody>
      </table>
    </div>
    <% if @pagy.pages > 1 %>
      <div class="mt-3 ms-3">
        <%== pagy_bootstrap_nav(@pagy) %>
      </div>
    <% end %>
  </div>
 <% end %>
<% end %>
