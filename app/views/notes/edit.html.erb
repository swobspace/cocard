<%= render ModalComponent.new(title: t('wobapphelpers.actions.edit', 
                                        model: t(@note.type, scope: 'notes'))) do %>
  <%= render 'form', note: @note, notable: @notable  %>

<% end %>
