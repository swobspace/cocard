<%= render ModalComponent.new(
      title: if @note.type == 'acknowledge'
               "Acknowledge"
             elsif @note.with_mail
               "Notiz mit Mail"
             else
               t(@note.type, scope: 'notes')
             end
    ) do %>
  <%= render 'form', notable: @notable, note: @note %>
<% end %>
