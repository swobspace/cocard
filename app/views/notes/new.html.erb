<%= render ModalComponent.new(title: t('wobapphelpers.actions.new', 
                                       model: t(@note.type, scope: 'notes'))) do %>
  <%= render 'form', notable: @notable, note: @note %>
<% end %>
