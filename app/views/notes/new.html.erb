<%= render ModalComponent.new(
      title: if @note.subject.present?
               "Neue Mail"
             else
               t('wobapphelpers.actions.new', model: t(@note.type, scope: 'notes'))
             end
    ) do %>
  <%= render 'form', notable: @notable, note: @note %>
<% end %>
