<div class="card mb-2">
  <div class="card-header">
    <div class="d-flex w-100 justify-content-between">
      <div>
        <span class="subtitle">
          <%= t('activerecord.models.operational_state') %>: 
          #<%= @operational_state.id %>
        </span>
        <h4 class="title"><%= @operational_state %></h4>
      </div>
      <div role="toolbar" class="pt-3">
        <%= edit_link(@operational_state) %>
        <%= delete_link(@operational_state) %>
      </div>
    </div>
  </div>

  <div class="card-body">
    <table class="table table-sm table-borderless">
    
      <tr>
        <th class="w-10 pe-3"><%= t('attributes.name')%>:</th>
        <td><%= @operational_state.name %></td>
      </tr>

      <tr>
        <th class="w-10 pe-3"><%= t('attributes.description')%>:</th>
        <td><%= @operational_state.description %></td>
      </tr>
    </table>
  </div>
</div>

<%= back_link %>
<%= new_link OperationalState %>
