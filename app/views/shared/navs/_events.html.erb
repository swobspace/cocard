<% if can? :navigate, :events %>
  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarEventsDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">

      <%= t('controller.events') %>
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarEventsDropdown">
      <%= link_to raw( '<span class="fas fa-fw fa-table" aria-hidden="true"></span> ' +
                       t('controller.logs')), 
                  main_app.logs_path, class: "dropdown-item" %>
     <% if false # comming soon %>
      <div class="dropdown-divider"></div>
        <%= link_to raw( '<span class="fas fa-fw fa-table" aria-hidden="true"></span> ' +
                         t('controller.events')), 
                    '#', class: "dropdown-item" %>
     <% end %>
    </div>

  </li>
<% end %>


